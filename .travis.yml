language: ruby
rvm:
 - "2.2.2"
services:
  - postgresql
before_script:
  - psql -c 'create database draft-tracker_test;' -U postgres

test:
  adapter: postgresql
  database: draft-tracker_test
# uncomment this line if your project needs to run something other than `rake`:
script: bundle exec rspec spec